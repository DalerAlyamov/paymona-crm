import React from 'react'
import styles from '../scss/organisms/Sidebar.module.scss'

const SideBar = () => {

  const [activeSideBar, setActiveSideBar] = useState(initialState);

  return (
    <div className={styles.root}>
      ergeg
    </div>
  )
}

export default SideBar
