import React from 'react'
import styles from '../scss/organisms/Sidebar.module.scss'

const SideBar = () => {

  const [ActiveSide, setActiveSide] = useState(initialState);

  return (
    <div className={styles.root}>
      ergeg
    </div>
  )
}

export default SideBar
